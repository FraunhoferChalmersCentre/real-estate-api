get:
  summary: Substation information
  operationId: getSubstationById
  description: Information for a specific substation
  tags:
    - substations
  parameters:
    - $ref: "../parameters/path/substation_id.yaml"
  responses:
    '200':
      description: Expected response to a valid request
      content:
        application/json:
          schema:
            $ref: "../schemas/Substation.yaml"
    default:
      $ref: "../responses/UnexpectedError.yaml"